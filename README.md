Onehalf theme based on [lush](https://github.com/rktjmp/lush.nvim)

# Usage

Example with packer: 

```lua
use({
  "zorgick/onehalf-lua",
  requires = {
    "rktjmp/lush.nvim",
  },
  disable = false,
}),
```
